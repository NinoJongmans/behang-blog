{% macro card(post) %}
    <a class="post-card{% if post.featured %}  post-card-featured{% endif %}" href="{{ post.url }}">
        <header class="post-card-header">
            {% if post.feature_image %}
                <img class="post-card-image" src="{{ post.feature_image }}" alt="{{ post.title }}">
            {% endif %}
            
        </header>
        <div class="post-card-excerpt">
            <div class="post-featured-tag">
                {% if post.featured %}<p class="spotlight-tag">Momenteel in de spotlight:</p>{% endif %}
            </div>
            <div class="post-card-tags">
                {% if post.tags %}{% for tag in post.tags %}{{ tag.name }} {% endfor %}{% endif %}
            </div>
            <h2 class="post-card-title">
                {{ post.title }}
            </h2>
            <p>{{ post.excerpt }}</p>
            <div class="post-card-footer-left">
                
            </div>
            <div class="post-card-footer-right">
                <div>{{ post.html | safe | getReadingTime }} minuten leestijd</div>  
            </div>
        </div>
    </a>
{% endmacro %}


/*<div class="post-card-excerpt">
            <p>{{ post.excerpt }}</p>
        </div>
        <footer class="post-card-footer">
            <div class="post-card-footer-left">
                
            </div>
            <div class="post-card-footer-right">
                <div>{{ post.html | safe | getReadingTime }} minuten leestijd</div>
            </div>
        </footer>*/






        <div class="post-card-tags">
                {% if post.tags %}{% for tag in post.tags %}{{ tag.name }} {% endfor %}{% endif %}
            </div>
            <h2 class="post-card-title">
                {{ post.title }}
            </h2>
            <p>{{ post.excerpt }}</p>
            <div class="post-card-footer-left">
                
            </div>
            <div class="post-card-footer-right">
                <div>{{ post.html | safe | getReadingTime }} minuten leestijd</div>
            </div>